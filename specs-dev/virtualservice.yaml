apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: post-custody-fn-vs
  namespace: fission
spec:
  hosts:
    - dev.api.siga.me
  gateways:
    - develop/dev-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/ticket/custody_transfer
    rewrite:
      uri: /ticket/custody_transfer
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80